---
description: 记忆协议 - OpenClaw 风格自动记忆召回
globs: ["**/*"]
alwaysApply: true
---

# 记忆协议

## 何时必须搜索记忆

当用户提到以下内容时，**必须先搜索记忆再回答**：

- 🔍 **时间词**："之前"、"以前"、"上次"、"最近"、"昨天"、"记得"
- 🔍 **延续词**："继续"、"接着"、"那个"、"刚才"
- 🔍 **询问历史**："什么时候"、"为什么这样"、"怎么实现的"
- 🔍 **偏好/习惯**："我的偏好"、"我通常"、"按照我的习惯"
- 🔍 **项目背景**："这个功能"、"这个设计"、"为什么选择"

## 如何搜索记忆

使用 Shell 工具（memory-tool.ts）：

```bash
# 语义搜索（支持 FTS5 关键词 + 向量语义）
bun run ~/work/cursor/cursor-remote-control/shared/memory-tool.ts search "关键词"

# 查看最近 N 天的记忆摘要
bun run ~/work/cursor/cursor-remote-control/shared/memory-tool.ts recent --days 3

# 写入今日日记（重要信息需手动记录）
bun run ~/work/cursor/cursor-remote-control/shared/memory-tool.ts write "内容"
```

## 记忆存储位置

- `.cursor/MEMORY.md` — 长期记忆（重要决策、用户偏好、技术选型）
- `.cursor/memory/*.md` — 日记（每日上下文、进展、讨论）
- `.cursor/memory/heartbeat-state.json` — 心跳状态追踪
- `.cursor/sessions/*.jsonl` — 会话历史（原始对话转录）
- `.memory.sqlite` — 向量数据库（FTS5 索引 + Embedding）

## 记忆写入时机

- **自动写入**：心跳系统每 30 分钟检查，自动抽取重要信息
- **手动写入**：遇到重大决策、用户明确偏好时，立即记录

## 示例

**用户**："你记得之前我们讨论的心跳系统吗？"
**正确做法**：
1. 先调用 `memory-tool.ts search "心跳系统"`
2. 阅读返回的记忆片段
3. 基于记忆内容回答

**错误做法**：
❌ 直接凭印象回答（会话上下文有限，容易遗忘）
